<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Theme applied while the app is launching (Splash screen) -->
    <style name="LaunchTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Show a splash background until Flutter UI loads -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <!-- Main app theme (used after Flutter starts) -->
    <style name="NormalTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Set solid background color to avoid Stripe initialization issues -->
        <item name="android:windowBackground">@android:color/white</item>

        <!-- Primary colors -->
        <item name="colorPrimary">@android:color/holo_blue_light</item>
        <item name="colorPrimaryVariant">@android:color/holo_blue_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Secondary colors -->
        <item name="colorSecondary">@android:color/holo_green_light</item>
        <item name="colorOnSecondary">@android:color/black</item>

        <!-- Optional: accent color -->
        <item name="colorAccent">@android:color/holo_orange_light</item>

        <!-- Optional: status bar color -->
        <item name="android:statusBarColor">@android:color/holo_blue_dark</item>
    </style>

</resources> 
